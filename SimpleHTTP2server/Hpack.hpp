#ifndef HPACK_HPP
#define HPACK_HPP

#include <string>
#include <iostream>
#include <string>

class Hpack {
private:

  
public:
  
  
  Hpack();
  
  void decodeHeader(char *buffer, unsigned int length);
};

#endif